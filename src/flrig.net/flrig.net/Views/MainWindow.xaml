<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:flrig.net.ViewModels;assembly=flrig.net"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="flrig.net.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="flrig.net">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>
    <Grid RowDefinitions="Auto,*,Auto" ColumnDefinitions="Auto,*,Auto">
        <TextBlock Text="{Binding Greeting}" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Grid.ColumnSpan="3"/>
        <Button Background="Blue" Content="{Binding Greeting}" Grid.Row="1" Grid.Column="1" />
        <Button Background="Blue" Content="{Binding Greeting}" Grid.Row="2" Command="{Binding OnClickCommand}" />
    </Grid>
</Window>
